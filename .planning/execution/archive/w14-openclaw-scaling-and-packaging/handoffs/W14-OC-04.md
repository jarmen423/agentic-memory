# W14-OC-04 Handoff

## Status

- Completed

## What Changed

- Updated `D:\code\agentic-memory\package.json`
  - root npm scripts now target the OpenClaw package by workspace path
    (`packages/am-openclaw`) instead of by the current package name string
  - added explicit convenience scripts:
    - `build:openclaw`
    - `test:openclaw`
    - `typecheck:openclaw`
    - `pack:openclaw`
    - `validate:release-artifacts`
- Updated `D:\code\agentic-memory\.github\workflows\ci.yml`
  - OpenClaw package tests/build/typecheck now use the workspace path
  - added `npm pack --workspace "$OPENCLAW_WORKSPACE" --dry-run`
  - added `docker compose -f docker-compose.prod.yml config`
  - this removes the final package-name decision from the CI critical path

## Verified

- `python -m pytest tests/test_am_server.py -vv`
- `python -m pytest tests/test_openclaw_contract.py -q`
- `python -m pytest desktop_shell/tests/test_app.py -q`
- `python -m pytest tests/e2e/test_openclaw_e2e.py tests/load/test_openclaw_load.py tests/chaos/test_openclaw_chaos.py -q`
- `npm run build`
- `npm run typecheck`
- `npm run build:openclaw`
- `npm run test:openclaw`
- `npm run typecheck:openclaw`
- `npm run build --workspace am-dashboard`
- `npm run test --workspace am-dashboard`
- `npm run typecheck --workspace am-dashboard`
- `npm run pack:openclaw`
- `npm run validate:release-artifacts`

## Risks / Notes

- `tests/test_am_server.py` passes but is slow in this environment. The `-q`
  form repeatedly exceeded shorter automation timeouts, while `-vv` completed
  in about eight minutes and showed all 52 tests passing.
- `docker compose config` still expands live environment values, so compose
  validation output should be treated as sensitive.
- Final npm publish naming is still unresolved, but that is now a publish-time
  decision rather than a local verification blocker.
