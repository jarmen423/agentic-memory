# W16-OC-01 Handoff

## Status

- Completed

## What Changed

- Added a backend onboarding-contract health endpoint:
  - `D:\code\agentic-memory\src\am_server\routes\health.py`
- Added explicit response models for required vs optional services and rollout
  readiness:
  - `D:\code\agentic-memory\src\am_server\models.py`
- Updated the desktop shell bootstrap route so it probes the backend contract
  instead of only echoing config:
  - `D:\code\agentic-memory\desktop_shell\app.py`
- Added shell tests covering:
  - healthy backend contract bootstrap
  - unreachable backend bootstrap without shell failure
  - `D:\code\agentic-memory\desktop_shell\tests\test_app.py`
- Updated the task registry to mark `W16-OC-01` complete:
  - `D:\code\agentic-memory\.planning\execution\tasks.json`

## Contract Locked In This Task

- Supported install command:
  - `openclaw plugin install agentic-memory-openclaw`
- Supported setup command:
  - `openclaw agentic-memory setup`
- Planned doctor command for the next task:
  - `openclaw agentic-memory doctor`
- Required services for honest setup/capture readiness:
  - backend HTTP API
  - backend API authentication
  - OpenClaw memory capture pipeline
- Optional services:
  - OpenClaw context engine
  - hosted MCP surfaces
  - desktop shell
  - temporal / SpacetimeDB local stack
  - Grafana
  - dedicated MCP keys when strict MCP auth is enabled

## Readiness Rules Locked

- `setup_ready`
  - backend HTTP API healthy
  - backend API auth configured
- `capture_only_ready`
  - `setup_ready`
  - OpenClaw memory pipeline healthy
- `augment_context_ready`
  - `capture_only_ready`
  - OpenClaw context engine healthy

## Verification

- Passed:
  - `python -m pytest desktop_shell/tests/test_app.py -q`
- Passed backend smoke:
  - `python -c "from fastapi.testclient import TestClient; from am_server.app import create_app; client=TestClient(create_app()); resp=client.get('/health/onboarding'); print(resp.status_code)"`

## Notes For W16-OC-02

- The backend now exposes one source-of-truth onboarding contract at:
  - `/health/onboarding`
- The plugin doctor/setup task should consume that endpoint instead of guessing:
  - which services are required
  - whether capture-only is truly ready
  - whether context mode is available
- The shell bootstrap now degrades cleanly when the backend is missing or too old
  to expose the new contract.
