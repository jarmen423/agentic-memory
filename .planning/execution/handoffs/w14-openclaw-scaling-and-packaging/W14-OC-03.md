# W14-OC-03 Handoff

## Status

- Completed

## What Changed

- Added `D:\code\agentic-memory\.github\workflows\release.yml`
  - manual-first release workflow for the OpenClaw package
  - package build, test, typecheck, and pack steps
  - artifact upload for the package tarball, deployment docs, and compose file
  - optional npm publish step gated by `publish_to_npm` plus an explicit
    `npm_package_name` input
- Added `D:\code\agentic-memory\docker-compose.prod.yml`
  - controlled-beta production scaffold for Neo4j plus `am-server`
  - persistent volumes for Neo4j data and local product state
  - healthchecks for both services
  - optional hosted-MCP and temporal-bridge environment wiring
- Added `D:\code\agentic-memory\docs\openclaw\README.md`
- Added `D:\code\agentic-memory\docs\openclaw\DEPLOYMENT_RUNBOOK.md`
- Added `D:\code\agentic-memory\docs\openclaw\BETA_ROLLOUT.md`
  - deployment steps, env expectations, rollout gates, and the note that the
    final npm package name remains a publish-time decision

## Verified

- `docker compose -f docker-compose.prod.yml config`
- `Get-Content .github/workflows/release.yml`
- `python -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml', 'r', encoding='utf-8')); print('release.yml parses as YAML')"`

## Risks / Notes

- The workflow deliberately keeps npm publish optional because the final package
  name is not locked yet. That means build/pack/release artifacts can proceed
  now without pretending the publish target is final.
- `docker compose config` resolves actual environment values. The runbook now
  warns operators to treat rendered config output as sensitive.
- This task only validated compose rendering, not a full container image build
  and deploy cycle. If the repo Dockerfile changes later, `W14-OC-04` should
  keep the compose/render gate and may choose to add a fuller build smoke test.
