# W15-OC-04 Handoff

## Status

- Completed

## What Changed

- Rewrote the top-level OpenClaw docs index so it points to the actual private
  beta surfaces:
  - `D:\code\agentic-memory\docs\openclaw\README.md`
- Added CI validation for the private-beta doc bundle and committed OpenAPI
  contract:
  - `D:\code\agentic-memory\.github\workflows\ci.yml`
- Added matching release-workflow validation so package artifacts cannot be
  built from a repo state that is missing the doc bundle or an aligned OpenAPI
  contract:
  - `D:\code\agentic-memory\.github\workflows\release.yml`

## Integration Outcome

- the OpenClaw docs root now reflects:
  - quickstart
  - deployment
  - OpenAPI
  - beta ops
  - support
  - marketplace/publishing
- CI now fails if those core private-beta docs or the committed OpenAPI artifact
  disappear
- CI and release automation now both validate that the live FastAPI app still
  exposes `/openclaw/memory/search`

## Verified

- `python -m pytest tests/test_am_server.py tests/test_openclaw_contract.py desktop_shell/tests/test_app.py -q`
- `python -m pytest tests/e2e/test_openclaw_e2e.py tests/load/test_openclaw_load.py tests/chaos/test_openclaw_chaos.py -q`
- `npm run build`
- `npm run typecheck`
- `npm run build:openclaw`
- `npm run test:openclaw`
- `npm run typecheck:openclaw`
- `npm run build --workspace am-dashboard`
- `npm run test --workspace am-dashboard`
- `npm run typecheck --workspace am-dashboard`
- `npm run pack:openclaw`
- `npm run validate:release-artifacts`
- `python -c "from am_server.app import create_app; spec = create_app().openapi(); assert '/openclaw/memory/search' in spec['paths']; print('openapi ok')"`

## Notes

- `python -m pytest tests/test_am_server.py tests/test_openclaw_contract.py desktop_shell/tests/test_app.py -q`
  is still the slowest gate on this machine and took about 18 minutes.
- The same pre-existing local warnings remain visible during Python validation:
  - `requests` dependency warning
  - `pytest_asyncio` fixture loop-scope deprecation warning
- `docker compose -f docker-compose.prod.yml config` renders interpolated local
  environment values, so that output should be treated as sensitive.
