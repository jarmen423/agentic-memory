# W16-OC-05 Handoff

## Status

- Completed

## What Changed

- Added backend regression coverage for the public onboarding contract:
  - `D:\code\agentic-memory\tests\test_am_server.py`
  - `D:\code\agentic-memory\tests\test_openclaw_contract.py`
- Added CI checks so the onboarding docs cannot drift away from the supported
  `doctor -> setup` path:
  - `D:\code\agentic-memory\.github\workflows\ci.yml`
- Added the same doctor-first and explicit-`STDB_URI` checks to the release
  workflow:
  - `D:\code\agentic-memory\.github\workflows\release.yml`

## Behavior Locked In This Task

- `/health/onboarding` is now part of the integration gate instead of being a
  docs-only promise.
- CI and release validation now check:
  - the OpenClaw onboarding docs mention `openclaw agentic-memory doctor`
  - the whole-stack temporal docs mention `STDB_URI`
  - the live app still exposes the locked onboarding contract fields:
    - `plugin_package_name`
    - `doctor_command`
- The whole-stack onboarding wave now has all task ids completed:
  - `W16-OC-00`
  - `W16-OC-01`
  - `W16-OC-02`
  - `W16-OC-03`
  - `W16-OC-04`
  - `W16-OC-05`

## Verification

- Passed:
  - `python -m pytest tests/test_am_server.py tests/test_openclaw_contract.py desktop_shell/tests/test_app.py -q`
  - `npm run build`
  - `npm run typecheck`
  - `npm run build:openclaw`
  - `npm run test:openclaw`
  - `npm run typecheck:openclaw`
  - `npm run build --workspace am-dashboard`
  - `npm run test --workspace am-dashboard`
  - `npm run typecheck --workspace am-dashboard`
  - `npm run build --workspace am-temporal-kg`
  - `npm run typecheck --workspace am-temporal-kg`
  - `npm run build --workspace am-sync-neo4j`
  - `npm run typecheck --workspace am-sync-neo4j`
  - `npm run pack:openclaw`
  - `npm run validate:release-artifacts`

## Residual Notes

- `tests/test_am_server.py` remains the slowest part of the Python gate on this
  machine and needed a longer timeout to complete.
- Two pre-existing warning classes remain during the Python gate:
  - `requests` dependency warning
  - `pytest_asyncio` loop-scope deprecation warning
- `docker compose -f docker-compose.prod.yml config` renders local environment
  values during validation. Treat that output as sensitive in logs and support
  workflows.

## Next Step

- The execution registry for Phase 16 can now be closed at the planning layer.
