---
phase: 02-web-research-core
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/am_server/__init__.py
  - src/am_server/app.py
  - src/am_server/auth.py
  - src/am_server/dependencies.py
  - src/am_server/models.py
  - src/am_server/routes/__init__.py
  - src/am_server/routes/health.py
  - src/am_server/routes/research.py
  - src/am_server/routes/ext.py
  - src/am_server/data/selectors.json
  - src/am_server/server.py
  - tests/test_am_server.py
  - pyproject.toml
autonomous: true
requirements:
  - "POST /ingest/research REST endpoint"
  - "GET /search/research REST endpoint"
  - "GET /ext/selectors.json unauthenticated endpoint"
  - "Bearer token API key authentication middleware"
  - "FastMCP ASGI mount at /mcp"
  - "GET /health unauthenticated endpoint"

must_haves:
  truths:
    - "GET /health returns 200 {'status': 'ok'} without any auth header"
    - "POST /ingest/research with valid Bearer token delegates to ResearchIngestionPipeline.ingest() and returns 202"
    - "POST /ingest/research without Authorization header returns 403"
    - "POST /ingest/research with wrong Bearer token returns 401"
    - "GET /search/research?q=...&limit=5 with valid Bearer token returns {'results': [...]}"
    - "GET /ext/selectors.json returns {'version': 1, 'platforms': {...}} without any auth header"
    - "FastMCP SSE app is mounted at /mcp (requests to /mcp return non-404)"
    - "require_auth raises 503 when AM_SERVER_API_KEY env var is not set"
    - "session_id comes from request body, never generated server-side"
  artifacts:
    - path: "src/am_server/app.py"
      provides: "FastAPI app factory with FastMCP mount at /mcp and router registration"
      exports: ["create_app"]
    - path: "src/am_server/auth.py"
      provides: "Bearer token auth dependency using HTTPBearer"
      exports: ["require_auth"]
    - path: "src/am_server/models.py"
      provides: "Pydantic request/response models for research endpoints"
      exports: ["ResearchIngestRequest", "CitationModel", "FindingModel"]
    - path: "src/am_server/dependencies.py"
      provides: "Singleton pipeline factory with lru_cache"
      exports: ["get_pipeline"]
    - path: "src/am_server/routes/research.py"
      provides: "POST /ingest/research and GET /search/research endpoints"
    - path: "src/am_server/routes/ext.py"
      provides: "GET /ext/selectors.json unauthenticated endpoint"
    - path: "src/am_server/routes/health.py"
      provides: "GET /health unauthenticated endpoint"
    - path: "src/am_server/data/selectors.json"
      provides: "Stub DOM selectors for am-ext (version 1, empty platforms)"
      contains: '"version": 1'
    - path: "tests/test_am_server.py"
      provides: "TDD tests covering all must-have truths"
    - path: "pyproject.toml"
      provides: "fastapi and uvicorn dependencies, am_server in wheel packages"
      contains: "fastapi>=0.115.0"
  key_links:
    - from: "src/am_server/app.py"
      to: "src/codememory/server/app.py"
      via: "imports mcp instance for ASGI mount"
      pattern: "from codememory\\.server\\.app import mcp"
    - from: "src/am_server/routes/research.py"
      to: "src/am_server/dependencies.py"
      via: "Depends(get_pipeline) for ResearchIngestionPipeline singleton"
      pattern: "get_pipeline"
    - from: "src/am_server/routes/research.py"
      to: "src/codememory/web/pipeline.py"
      via: "pipeline.ingest() dispatched via run_in_executor"
      pattern: "run_in_executor.*ingest"
    - from: "src/am_server/routes/ext.py"
      to: "src/am_server/data/selectors.json"
      via: "reads JSON file from disk"
      pattern: "selectors\\.json"
---

<objective>
Build the am-server FastAPI REST API foundation: a FastAPI application that co-hosts the existing FastMCP server via ASGI mount and exposes web research endpoints with Bearer token authentication.

Purpose: Unblocks Phase 4 (conversation endpoints), Phase 5 (am-proxy), and Phase 6 (am-ext) by providing the REST API infrastructure and auth middleware they will extend. Provides REST equivalents of MCP research tools for non-MCP clients.

Output: `src/am_server/` package with FastAPI app, auth middleware, research routes, selectors endpoint, health check, and full test suite.
</objective>

<execution_context>
@C:/Users/jfrie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jfrie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-web-research-core/02-CONTEXT.md
@.planning/phases/02-web-research-core/02-RESEARCH.md (section "02-04: REST API Foundation")

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/codememory/server/app.py:
```python
from mcp.server.fastmcp import FastMCP
mcp = FastMCP("Agentic Memory")
# mcp.sse_app() returns a Starlette ASGI instance — mount with FastAPI.mount()
```

From src/codememory/web/pipeline.py (created by 02-02):
```python
class ResearchIngestionPipeline(BaseIngestionPipeline):
    def ingest(self, source: dict) -> dict:
        # Synchronous — must dispatch via run_in_executor in async routes
        ...
```

From src/codememory/core/connection.py:
```python
class ConnectionManager:
    def __init__(self, uri: str, user: str, password: str): ...
```

From src/codememory/core/embedding.py:
```python
class EmbeddingService:
    def __init__(self, provider: str, api_key: str): ...
```

From src/codememory/core/entity_extraction.py:
```python
class EntityExtractionService:
    def __init__(self, api_key: str): ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TDD test suite + package skeleton + pyproject.toml deps + selectors stub</name>
  <files>
    tests/test_am_server.py
    src/am_server/__init__.py
    src/am_server/routes/__init__.py
    src/am_server/data/selectors.json
    src/am_server/models.py
    src/am_server/auth.py
    src/am_server/dependencies.py
    src/am_server/app.py
    src/am_server/routes/health.py
    src/am_server/routes/research.py
    src/am_server/routes/ext.py
    src/am_server/server.py
    pyproject.toml
  </files>
  <read_first>
    src/codememory/server/app.py
    pyproject.toml
    .planning/phases/02-web-research-core/02-RESEARCH.md (from line 631 — section "02-04: REST API Foundation")
  </read_first>
  <behavior>
    - test_health: GET /health returns 200 + {"status": "ok"} without auth header
    - test_ingest_no_auth: POST /ingest/research without Authorization header returns 403 (HTTPBearer behavior)
    - test_ingest_bad_token: POST /ingest/research with "Bearer wrong-key" returns 401
    - test_ingest_research_ok: POST /ingest/research with "Bearer test-key-abc" returns 202, pipeline.ingest() called with correct payload dict
    - test_ingest_delegates: Verify pipeline.ingest() receives model_dump() of request body with all fields
    - test_search_research_ok: GET /search/research?q=test&limit=5 with valid Bearer returns 200 + {"results": [...]}
    - test_search_no_auth: GET /search/research?q=test without auth returns 403
    - test_selectors_shape: GET /ext/selectors.json returns 200 + {"version": 1, "platforms": {...}}
    - test_selectors_no_auth: GET /ext/selectors.json without Authorization header returns 200 (not 401/403)
    - test_auth_missing_key: When AM_SERVER_API_KEY env var is unset, authenticated endpoint returns 503
    - test_mcp_mounted: GET /mcp returns non-404 (verifies ASGI mount exists)
  </behavior>
  <action>
    **Phase A — Skeleton and dependencies (do this first):**

    1. Update `pyproject.toml`:
       - Add `"fastapi>=0.115.0"` and `"uvicorn[standard]>=0.30.0"` to `dependencies` list
       - Update `[tool.hatch.build.targets.wheel]` packages to `["src/codememory", "src/am_server"]`
       - Add `[[tool.mypy.overrides]]` for `am_server.*` with `ignore_missing_imports = true`

    2. Create `src/am_server/__init__.py` — empty package marker

    3. Create `src/am_server/routes/__init__.py` — empty package marker

    4. Create `src/am_server/data/selectors.json`:
       ```json
       {"version": 1, "platforms": {}}
       ```
       Per planning context: Phase 6 will populate real DOM selectors. Stub returns minimal valid shape.

    5. Create `src/am_server/models.py` with Pydantic models:
       - `CitationModel(BaseModel)`: `url: str`, `title: str | None = None`, `snippet: str | None = None`
       - `FindingModel(BaseModel)`: `text: str`, `confidence: str | None = None`, `citations: list[CitationModel] = []`
       - `ResearchIngestRequest(BaseModel)`: `type: str`, `content: str`, `project_id: str`, `session_id: str`, `source_agent: str`, `title: str | None = None`, `research_question: str | None = None`, `confidence: str | None = None`, `findings: list[FindingModel] | None = None`, `citations: list[CitationModel] | None = None`

    6. Create `src/am_server/auth.py`:
       - Import `HTTPBearer`, `HTTPAuthorizationCredentials` from `fastapi.security`
       - `_bearer = HTTPBearer()` module-level instance
       - `def require_auth(credentials: HTTPAuthorizationCredentials = Security(_bearer)) -> str:`
         - Read `os.environ.get("AM_SERVER_API_KEY", "")`
         - If empty: raise `HTTPException(status_code=503, detail="AM_SERVER_API_KEY not configured")`
         - If `credentials.credentials != expected`: raise `HTTPException(status_code=401, detail="Invalid API key", headers={"WWW-Authenticate": "Bearer"})`
         - Return `credentials.credentials`

    7. Create `src/am_server/dependencies.py`:
       - `@lru_cache(maxsize=1)` on `def get_pipeline() -> ResearchIngestionPipeline:`
       - Inside: instantiate `ConnectionManager(uri=os.environ["NEO4J_URI"], user=os.environ["NEO4J_USER"], password=os.environ["NEO4J_PASSWORD"])`
       - Instantiate `EmbeddingService(provider="gemini", api_key=os.environ["GEMINI_API_KEY"])`
       - Instantiate `EntityExtractionService(api_key=os.environ["GROQ_API_KEY"])`
       - Return `ResearchIngestionPipeline(conn, embedder, extractor)`

    8. Create `src/am_server/routes/health.py`:
       - `router = APIRouter()`
       - `@router.get("/health")` returning `{"status": "ok"}`

    9. Create `src/am_server/routes/research.py`:
       - `router = APIRouter(dependencies=[Depends(require_auth)])`
       - `@router.post("/ingest/research", status_code=202)`:
         - Parameter: `body: ResearchIngestRequest`
         - Get pipeline via `get_pipeline()`
         - Dispatch sync `pipeline.ingest(body.model_dump())` via `await asyncio.get_event_loop().run_in_executor(None, pipeline.ingest, body.model_dump())`
         - Return `{"status": "ok", "result": result}`
       - `@router.get("/search/research")`:
         - Parameters: `q: str = Query(...)`, `limit: int = Query(10, ge=1, le=50)`
         - Get pipeline via `get_pipeline()`
         - Call pipeline's search method (or ConnectionManager vector query) via run_in_executor
         - Return `{"results": results}`

    10. Create `src/am_server/routes/ext.py`:
        - `router = APIRouter()` (NO auth dependency — intentionally unauthenticated)
        - `_SELECTORS_PATH = Path(__file__).parent.parent / "data" / "selectors.json"`
        - `@router.get("/ext/selectors.json")`:
          - Read and return `json.loads(_SELECTORS_PATH.read_text(encoding="utf-8"))`
          - On `FileNotFoundError`: raise `HTTPException(status_code=404, detail="selectors.json not found")`

    11. Create `src/am_server/app.py`:
        - `from contextlib import asynccontextmanager`
        - `@asynccontextmanager async def lifespan(app: FastAPI):` — calls `get_pipeline()` for fail-fast startup, then `yield`
        - `def create_app() -> FastAPI:` — creates FastAPI instance with title="am-server", version="0.1.0", lifespan=lifespan
        - `app.mount("/mcp", mcp.sse_app())` — import `mcp` from `codememory.server.app`
        - Include routers: health, research, ext
        - Return app

    12. Create `src/am_server/server.py`:
        - `def run() -> None:` — calls `create_app()`, then `uvicorn.run(app, host=os.environ.get("AM_SERVER_HOST", "0.0.0.0"), port=int(os.environ.get("AM_SERVER_PORT", "8765")))`
        - `if __name__ == "__main__": run()`

    **Phase B — Write failing tests (RED):**

    13. Create `tests/test_am_server.py` with these tests (all use `fastapi.testclient.TestClient`):

        Fixture `client(monkeypatch)`:
        - `monkeypatch.setenv("AM_SERVER_API_KEY", "test-key-abc")`
        - `monkeypatch.setenv("NEO4J_URI", "bolt://localhost:7687")`
        - `monkeypatch.setenv("NEO4J_USER", "neo4j")`
        - `monkeypatch.setenv("NEO4J_PASSWORD", "test")`
        - `monkeypatch.setenv("GEMINI_API_KEY", "fake-gemini")`
        - `monkeypatch.setenv("GROQ_API_KEY", "fake-groq")`
        - Patch `am_server.dependencies.ResearchIngestionPipeline` with MagicMock
        - Clear `dependencies.get_pipeline.cache_clear()`
        - Create app via `create_app()`, yield `TestClient(app, raise_server_exceptions=False)`

        Fixture `auth_headers` returns `{"Authorization": "Bearer test-key-abc"}`

        Tests:
        - `test_health(client)` — GET /health -> 200, `{"status": "ok"}`
        - `test_ingest_no_auth(client)` — POST /ingest/research without auth -> 403
        - `test_ingest_bad_token(client)` — POST with "Bearer wrong-key" -> 401
        - `test_ingest_research_ok(client, auth_headers)` — POST with valid token + full body -> 202
        - `test_ingest_delegates(client, auth_headers)` — POST verifies `pipeline.ingest.call_args[0][0]` contains `session_id`, `project_id`, `content`, `type`, `source_agent`
        - `test_search_research_ok(client, auth_headers)` — GET /search/research?q=test&limit=5 -> 200 with `{"results": [...]}`
        - `test_search_no_auth(client)` — GET /search/research?q=test without auth -> 403
        - `test_selectors_shape(client)` — GET /ext/selectors.json -> 200, body has "version" == 1 and "platforms" key
        - `test_selectors_no_auth(client)` — GET /ext/selectors.json without auth -> 200 (not 401/403)
        - `test_auth_missing_key(monkeypatch)` — Create client WITHOUT AM_SERVER_API_KEY env var. POST /ingest/research with "Bearer anything" -> 503
        - `test_mcp_mounted(client)` — GET /mcp -> status code != 404

    **Phase C — Run tests, fix until GREEN:**

    Run `pytest tests/test_am_server.py -x -q --tb=short` and fix any issues until all tests pass.

    **Important constraints:**
    - `session_id` MUST come from request body — never generate it server-side
    - `/ext/selectors.json` and `/health` MUST NOT have auth dependencies
    - Use `asyncio.get_event_loop().run_in_executor(None, ...)` for sync pipeline calls in async routes
    - HTTPBearer returns 403 on missing header, 401 on wrong token — tests MUST assert accordingly
  </action>
  <verify>
    <automated>pytest tests/test_am_server.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fastapi>=0.115.0" pyproject.toml
    - grep -q 'uvicorn\[standard\]>=0.30.0' pyproject.toml
    - grep -q "src/am_server" pyproject.toml
    - python -c "from am_server.app import create_app; print('ok')"
    - python -c "from am_server.auth import require_auth; print('ok')"
    - python -c "from am_server.models import ResearchIngestRequest; print('ok')"
    - python -c "from am_server.dependencies import get_pipeline; print('ok')"
    - python -c "import json; d=json.load(open('src/am_server/data/selectors.json')); assert d['version']==1; print('ok')"
    - pytest tests/test_am_server.py -x -q --tb=short passes (all 11 tests green)
  </acceptance_criteria>
  <done>
    All 11 test cases pass. FastAPI app factory creates a working app with: health check (unauthenticated), research ingest (auth required, 202 response, delegates to pipeline), research search (auth required), selectors endpoint (unauthenticated, returns stub JSON), MCP ASGI mount at /mcp. pyproject.toml updated with fastapi and uvicorn deps. am_server included in wheel packages.
  </done>
</task>

</tasks>

<verification>
```bash
# All unit tests pass
pytest tests/test_am_server.py -x -q --tb=short

# Package imports work
python -c "from am_server.app import create_app; print('ok')"

# pyproject.toml has new deps
grep "fastapi" pyproject.toml
grep "uvicorn" pyproject.toml
grep "am_server" pyproject.toml

# selectors.json exists and is valid
python -c "import json; d=json.load(open('src/am_server/data/selectors.json')); assert d['version']==1"

# Full test suite still passes (no regressions)
pytest tests/ -q --tb=short
```
</verification>

<success_criteria>
- 11 test cases in tests/test_am_server.py all pass
- FastAPI app with 5 endpoints: GET /health, POST /ingest/research, GET /search/research, GET /ext/selectors.json, mount /mcp
- Bearer auth protects /ingest/research and /search/research; rejects missing header (403), wrong token (401), unconfigured key (503)
- /health and /ext/selectors.json are unauthenticated
- FastMCP SSE app mounted at /mcp path
- pyproject.toml includes fastapi>=0.115.0 and uvicorn[standard]>=0.30.0
- src/am_server included in hatch wheel packages
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/02-web-research-core/02-04-SUMMARY.md`
</output>
