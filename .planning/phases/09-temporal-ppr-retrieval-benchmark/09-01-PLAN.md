---
phase: 09-temporal-ppr-retrieval-benchmark
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/codememory/temporal/__init__.py
  - src/codememory/temporal/bridge.py
  - src/am_server/dependencies.py
  - src/codememory/server/tools.py
  - src/codememory/chat/pipeline.py
  - src/codememory/web/pipeline.py
  - packages/am-temporal-kg/package.json
  - packages/am-temporal-kg/scripts/query_temporal.ts
autonomous: true
requirements:
  - BRIDGE-01
  - BRIDGE-02
  - POPULATE-01

must_haves:
  truths:
    - "Python has a cached TemporalBridge that manages one long-lived Node helper process"
    - "The Node helper uses generated SpacetimeDB bindings and the existing STDB env contract"
    - "The helper can resolve seed entities and execute temporal_ppr_retrieve without per-request process startup"
    - "Conversation and research ingestion paths can shadow-write into SpacetimeDB without breaking Neo4j-only ingest"
    - "Shadow writes are best-effort: bridge failures are logged and swallowed, not surfaced to passive connectors"
    - "Evidence rows use a stable sourceKind/sourceId contract that Python retrieval can hydrate later"
  artifacts:
    - path: "src/codememory/temporal/bridge.py"
      provides: "Python bridge lifecycle and JSON-lines RPC client"
      exports:
        - "TemporalBridge"
        - "get_temporal_bridge"
    - path: "packages/am-temporal-kg/scripts/query_temporal.ts"
      provides: "Long-lived Node helper for SpacetimeDB read/write operations"
      contains: "temporal_ppr_retrieve"
    - path: "src/codememory/chat/pipeline.py"
      provides: "Conversation shadow writes into SpacetimeDB"
      contains: "conversation_turn"
    - path: "src/codememory/web/pipeline.py"
      provides: "Research shadow writes into SpacetimeDB"
      contains: "research_finding"
  key_links:
    - from: "TemporalBridge.retrieve"
      to: "packages/am-temporal-kg/scripts/query_temporal.ts"
      via: "JSON lines request/response over stdin/stdout"
      pattern: "\"op\": \"retrieve\""
    - from: "ConversationIngestionPipeline"
      to: "TemporalBridge.ingest_relation"
      via: "best-effort shadow write with evidence sourceKind=conversation_turn"
      pattern: "conversation_turn"
    - from: "ResearchIngestionPipeline"
      to: "TemporalBridge.ingest_claim"
      via: "best-effort shadow write with evidence sourceKind=research_chunk|research_finding"
      pattern: "research_(chunk|finding)"
---

<objective>
Build the runtime bridge between the Python retrieval stack and the existing TypeScript
SpacetimeDB bindings, and add the minimum live shadow-write path required for Phase 9 retrieval
to operate on current conversation and research traffic.

Purpose: Phase 8 delivered the module and sync worker, but not a live Python->SpacetimeDB path.
Phase 9 retrieval cannot cut over cleanly until that gap is closed.

Output: New Python bridge, new TypeScript query helper, and best-effort shadow population from
conversation/research ingestion.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-temporal-ppr-retrieval-benchmark/09-CONTEXT.md
@.planning/phases/09-temporal-ppr-retrieval-benchmark/09-RESEARCH.md
@src/am_server/dependencies.py
@src/codememory/server/tools.py
@src/codememory/chat/pipeline.py
@src/codememory/web/pipeline.py
@packages/am-temporal-kg/src/procedures/retrieve.ts
@packages/am-temporal-kg/src/reducers/ingest.ts
@packages/am-temporal-kg/generated-bindings/index.ts
@packages/am-sync-neo4j/src/stdb_client.ts
@packages/am-sync-neo4j/src/config.ts
</context>

<interfaces>
Current env contract already in repo:
```text
STDB_URI
STDB_MODULE_NAME
STDB_BINDINGS_MODULE
STDB_CONFIRMED_READS
STDB_TOKEN
```

Existing procedure signature from generated bindings:
```ts
temporal_ppr_retrieve({
  projectId: string,
  seedNodeIds: bigint[],
  asOfUs: bigint,
  maxEdges: number,
  maxHops: number,
  alpha: number,
  halfLifeHours: number,
  minRelevance: number,
})
```

Recommended evidence contract from 09-RESEARCH.md:
```text
conversation_turn -> sourceId = "{session_id}:{turn_index}"
research_chunk    -> sourceId = "{source_key}:{content_hash}"
research_finding  -> sourceId = "{source_key}:{content_hash}"
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the Node helper and Python TemporalBridge</name>
  <files>
    packages/am-temporal-kg/package.json,
    packages/am-temporal-kg/scripts/query_temporal.ts,
    src/codememory/temporal/__init__.py,
    src/codememory/temporal/bridge.py,
    tests/test_temporal_bridge.py
  </files>
  <behavior>
    - Test: Python can start the helper lazily and send a JSON request without spawning a new
      process for each call.
    - Test: helper error responses become structured Python exceptions inside TemporalBridge.
    - Test: `get_temporal_bridge()` returns a cached singleton.
    - Test: bridge disable/misconfiguration is handled gracefully and can be checked by callers.
  </behavior>
  <action>
    Add `packages/am-temporal-kg/scripts/query_temporal.ts` as a long-lived stdio process.

    It should:
    1. load generated bindings from `STDB_BINDINGS_MODULE`,
    2. establish one warm SpacetimeDB connection,
    3. read and write JSON lines,
    4. support:
       - `retrieve`
       - `ingest_claim`
       - `ingest_relation`

    `retrieve` must:
    - resolve seed entities to node ids,
    - call `temporal_ppr_retrieve`,
    - hydrate node and evidence rows,
    - return compact JSON with timing metadata.

    Add an npm script in `packages/am-temporal-kg/package.json`:
    ```json
    "bridge": "tsx scripts/query_temporal.ts"
    ```

    Add `src/codememory/temporal/bridge.py` with:
    - `TemporalBridge`
    - `get_temporal_bridge()`

    The bridge should:
    - manage one child process,
    - expose `retrieve`, `ingest_claim`, `ingest_relation`, `is_available`, and `close`,
    - treat missing env vars as bridge-unavailable rather than fatal.
  </action>
  <verify>
    <automated>
      cd /d/code/agentic-memory && npm run typecheck --workspace am-temporal-kg && python -m pytest tests/test_temporal_bridge.py -x -q 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    TypeScript typecheck passes. The helper is callable. Python tests prove the bridge caches a
    single process and handles structured responses cleanly.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add best-effort shadow writes from conversation and research ingestion</name>
  <files>
    src/am_server/dependencies.py,
    src/codememory/server/tools.py,
    src/codememory/chat/pipeline.py,
    src/codememory/web/pipeline.py,
    tests/test_conversation_pipeline.py,
    tests/test_web_pipeline.py
  </files>
  <behavior>
    - Test: conversation ingest attempts a bridge shadow write for each extracted entity without
      failing the primary Neo4j ingest when the bridge is unavailable.
    - Test: research report/finding ingest attempts a bridge shadow write using the stable
      evidence sourceKind/sourceId contract.
    - Test: shadow-write exceptions are logged and swallowed.
    - Test: pipeline constructors remain backward compatible when no bridge is supplied.
  </behavior>
  <action>
    Make the conversation and research pipelines optionally accept `temporal_bridge`.

    Update:
    - `src/am_server/dependencies.py`
    - `src/codememory/server/tools.py`

    so the existing singleton factories inject `get_temporal_bridge()` when configured.

    In `src/codememory/chat/pipeline.py`, after the normal Neo4j write succeeds:
    - shadow-write a memory-item -> entity relation for each extracted entity
    - use subject kind `conversation_turn`
    - use `sourceKind = "conversation_turn"`
    - use `sourceId = f"{session_id}:{turn_index}"`

    In `src/codememory/web/pipeline.py`:
    - shadow-write memory-item -> entity relations for chunks and findings
    - shadow-write extracted claims via `ingest_claim`
    - use:
      - `sourceKind = "research_chunk"` or `"research_finding"`
      - `sourceId = f"{source_key}:{content_hash}"`

    Wrap all bridge calls in narrow `try/except` blocks and never let bridge failures break the
    primary Neo4j ingest path.
  </action>
  <verify>
    <automated>
      cd /d/code/agentic-memory && python -m pytest tests/test_conversation_pipeline.py tests/test_web_pipeline.py -x -q 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    Both pipelines keep their existing behavior and now perform best-effort shadow writes with the
    stable evidence contract needed for Phase 9 retrieval hydration.
  </done>
</task>

</tasks>

<verification>
```bash
cd /d/code/agentic-memory && npm run typecheck --workspace am-temporal-kg
python -m pytest tests/test_temporal_bridge.py tests/test_conversation_pipeline.py tests/test_web_pipeline.py -x -q
python -c "from codememory.temporal.bridge import get_temporal_bridge; print(type(get_temporal_bridge()).__name__)"
```
TypeScript helper typechecks. Python bridge tests pass. Conversation and research pipelines remain
green while exercising best-effort shadow writes.
</verification>

<success_criteria>
- `src/codememory/temporal/bridge.py` exists with a cached bridge singleton
- `packages/am-temporal-kg/scripts/query_temporal.ts` exists and supports `retrieve`,
  `ingest_claim`, and `ingest_relation`
- One long-lived Node helper process is reused instead of spawning per request
- Conversation and research ingestion paths can shadow-write into SpacetimeDB
- Evidence source contracts are stable and future retrieval can hydrate from them
- Bridge failures never break the existing Neo4j ingestion path
- All targeted Python tests pass and `npm run typecheck --workspace am-temporal-kg` passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-temporal-ppr-retrieval-benchmark/09-01-SUMMARY.md`
</output>
