---
phase: 09-temporal-ppr-retrieval-benchmark
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - src/codememory/temporal/seeds.py
  - src/codememory/server/tools.py
  - src/codememory/server/app.py
  - src/am_server/routes/conversation.py
  - tests/test_web_tools.py
  - tests/test_am_server.py
  - tests/test_server.py
autonomous: true
requirements:
  - RETRIEVE-01
  - RETRIEVE-02
  - FALLBACK-01

must_haves:
  truths:
    - "search_web_memory uses temporal PPR as the primary ranking path when seeds and bridge data are available"
    - "get_conversation_context uses temporal PPR as the primary ranking path when seeds and bridge data are available"
    - "GET /search/conversations returns temporal-ranked results while preserving its existing response shape"
    - "All three public surfaces keep deterministic fallback to the current vector/text behavior"
    - "Temporal seed discovery is entity-first and derived from current vector hits or query extraction"
    - "as_of is converted to microseconds for the temporal path and still works with fallback"
  artifacts:
    - path: "src/codememory/temporal/seeds.py"
      provides: "Seed collection and scoring helpers"
      exports:
        - "collect_seed_entities"
        - "parse_as_of_to_micros"
    - path: "src/codememory/server/app.py"
      provides: "Temporal-first web-memory retrieval"
      contains: "search_web_memory"
    - path: "src/codememory/server/tools.py"
      provides: "Temporal-first conversation-context retrieval"
      contains: "get_conversation_context"
    - path: "src/am_server/routes/conversation.py"
      provides: "Temporal-first REST conversation search"
      contains: "/search/conversations"
  key_links:
    - from: "search_web_memory"
      to: "TemporalBridge.retrieve"
      via: "seed discovery -> bridge call -> fallback on empty/error"
      pattern: "collect_seed_entities"
    - from: "get_conversation_context"
      to: "conversation_turn evidence hydration"
      via: "sourceId=session_id:turn_index"
      pattern: "conversation_turn"
    - from: "GET /search/conversations"
      to: "existing vector/text search"
      via: "explicit fallback path preserved"
      pattern: "falling back"
---

<objective>
Cut the public retrieval surfaces over to temporal PPR as the primary ranking path while preserving
their current API contracts and fallback behavior.

Purpose: Phase 9 is complete only when users hit the existing MCP and REST entry points and get
temporal-aware results without losing compatibility.

Output: Updated MCP and REST retrieval code with seed discovery, bridge calls, evidence hydration,
and deterministic fallback.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-temporal-ppr-retrieval-benchmark/09-CONTEXT.md
@.planning/phases/09-temporal-ppr-retrieval-benchmark/09-RESEARCH.md
@.planning/phases/09-temporal-ppr-retrieval-benchmark/09-01-SUMMARY.md
@src/codememory/server/tools.py
@src/codememory/server/app.py
@src/am_server/routes/conversation.py
@src/codememory/chat/pipeline.py
@src/codememory/web/pipeline.py
@src/codememory/temporal/bridge.py
</context>

<interfaces>
Current MCP `get_conversation_context(...)` return shape:
```python
{"query": str, "turns": [ ... ]}
```

Current MCP `search_web_memory(...)` return shape:
```python
"Found N relevant research result(s): ..."
```

Current REST `/search/conversations` return shape:
```python
{"results": [ ... ]}
```

Recommended seed algorithm from 09-RESEARCH.md:
1. run current vector search
2. collect `(entity_name, entity_type)` pairs from result metadata
3. if empty, run query entity extraction
4. if still empty, fallback immediately
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared seed-discovery and temporal hydration helpers</name>
  <files>
    src/codememory/temporal/seeds.py,
    tests/test_server.py,
    tests/test_web_tools.py
  </files>
  <behavior>
    - Test: top vector hits with entities/entity_types produce deterministic ranked seed entities.
    - Test: empty-hit seed collection returns [] cleanly.
    - Test: invalid `as_of` strings are handled predictably and do not crash the retrieval path.
    - Test: conversation evidence `sourceId=session:turn` can be parsed into hydration keys.
  </behavior>
  <action>
    Create `src/codememory/temporal/seeds.py` with helpers:
    - `collect_seed_entities(rows: list[dict], limit: int = 5) -> list[dict]`
    - `extract_query_seed_entities(query: str, extractor: EntityExtractionService) -> list[dict]`
    - `parse_as_of_to_micros(as_of: str | None) -> int | None`
    - `parse_conversation_source_id(source_id: str) -> tuple[str, int]`

    Keep this module pure-Python and side-effect free so all public surfaces can share it.

    Update the baseline vector projections where needed so the seed path has access to:
    - `entities`
    - `entity_types`
    - stable source identity fields
  </action>
  <verify>
    <automated>
      cd /d/code/agentic-memory && python -m pytest tests/test_server.py tests/test_web_tools.py -x -q 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    Shared helpers exist and are used by multiple retrieval surfaces. Seed scoring and `as_of`
    parsing are tested.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cut over MCP and REST retrieval to temporal-first behavior with fallback</name>
  <files>
    src/codememory/server/tools.py,
    src/codememory/server/app.py,
    src/am_server/routes/conversation.py,
    tests/test_web_tools.py,
    tests/test_am_server.py
  </files>
  <behavior>
    - Test: `search_web_memory` calls the bridge when seeds are available and formats temporal
      evidence into the existing string-style output.
    - Test: `get_conversation_context` returns the same top-level dict shape while sourcing
      ranked turns from temporal evidence when available.
    - Test: `/search/conversations` returns the same `{"results": [...]}` shape and falls back
      to the current vector/text path on bridge errors.
    - Test: fallback remains active on bridge unavailable, no seeds, empty temporal result, or
      bridge exception.
  </behavior>
  <action>
    **A. Update `search_web_memory` in `src/codememory/server/app.py`.**

    Flow:
    1. run the current Neo4j vector search as seed discovery
    2. collect seed entities
    3. if needed, use query entity extraction as a second seed source
    4. call `TemporalBridge.retrieve(...)`
    5. if temporal results exist, format them into the current user-facing string response
    6. otherwise return the current vector baseline output

    **B. Update `get_conversation_context` in `src/codememory/server/tools.py`.**

    Keep return shape:
    ```python
    {"query": query, "turns": turns}
    ```

    Temporal path:
    - discover seeds from matched turns or query extraction
    - call bridge
    - hydrate `conversation_turn` evidence rows back into Neo4j turn nodes using
      `session_id + turn_index`
    - keep surrounding-turn context-window fetch logic after temporal ranking

    **C. Update REST `GET /search/conversations` in `src/am_server/routes/conversation.py`.**

    Add optional query parameter:
    ```python
    as_of: str | None = Query(None, description="Optional ISO-8601 temporal cutoff")
    ```

    Use the same orchestration pattern as the MCP surface:
    - baseline vector hits for seed discovery
    - bridge call
    - hydrate turns from evidence
    - return `{"results": [...]}` in the same field shape as today
    - fallback to existing vector and then text search on failure

    **D. Logging and failure behavior**
    - log server-side when fallback is used and why
    - never surface temporal-path failures as API-shape changes
  </action>
  <verify>
    <automated>
      cd /d/code/agentic-memory && python -m pytest tests/test_web_tools.py tests/test_am_server.py -x -q 2>&1 | tail -20
    </automated>
  </verify>
  <done>
    MCP and REST retrieval are temporal-first, but callers still see stable response contracts and
    deterministic fallback on any temporal-path problem.
  </done>
</task>

</tasks>

<verification>
```bash
cd /d/code/agentic-memory && python -m pytest tests/test_web_tools.py tests/test_am_server.py tests/test_server.py -x -q
python -c "from codememory.temporal.seeds import collect_seed_entities, parse_as_of_to_micros; print('seed helpers ok')"
```
All targeted tests pass. Shared seed helpers import cleanly. MCP and REST retrieval keep their
existing output shapes.
</verification>

<success_criteria>
- `search_web_memory` uses temporal PPR as primary retrieval when seeds and bridge data exist
- `get_conversation_context` uses temporal PPR as primary retrieval when seeds and bridge data exist
- `GET /search/conversations` uses temporal PPR as primary retrieval when seeds and bridge data exist
- All three surfaces preserve their current top-level response contracts
- `as_of` is honored through the temporal path and fallback path
- Server-side logging explains fallback reasons
- All targeted Python tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/09-temporal-ppr-retrieval-benchmark/09-02-SUMMARY.md`
</output>
