---
phase: 09-temporal-ppr-retrieval-benchmark
plan: "03"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - package.json
  - bench/README.md
  - bench/build_temporal_kg.ts
  - bench/run_queries.ts
  - bench/measure_tokens.ts
  - bench/report_results.ts
  - bench/fixtures/smoke-traces.jsonl
autonomous: true
requirements:
  - BENCH-01
  - BENCH-02
  - BENCH-03

must_haves:
  truths:
    - "The benchmark harness can populate SpacetimeDB from replayed traces without relying on live runtime traffic"
    - "The same query set is executed against baseline vector retrieval and temporal PPR retrieval"
    - "The report includes token estimates, latency, fallback usage, and temporal consistency verdicts"
    - "The harness supports user-supplied real traces and also ships a tiny smoke fixture for local verification"
    - "Benchmark outputs are written as Markdown and machine-readable JSON"
  artifacts:
    - path: "bench/build_temporal_kg.ts"
      provides: "Replay-based SpacetimeDB population"
      contains: "ingest_temporal_claim"
    - path: "bench/run_queries.ts"
      provides: "Baseline vs temporal query execution"
      contains: "temporal_ppr_retrieve"
    - path: "bench/measure_tokens.ts"
      provides: "Repo-consistent token estimation"
      contains: "1.3"
    - path: "bench/report_results.ts"
      provides: "Markdown and JSON benchmark report generation"
      contains: "token_reduction_pct"
  key_links:
    - from: "build_temporal_kg.ts"
      to: "query_temporal.ts or generated bindings"
      via: "replay traces directly into SpacetimeDB reducers"
      pattern: "ingest_(temporal_claim|temporal_edge)"
    - from: "run_queries.ts"
      to: "baseline vector retrieval"
      via: "same query corpus against two retrieval modes"
      pattern: "baseline"
    - from: "report_results.ts"
      to: "bench/results/phase-09-report.md"
      via: "Markdown output plus JSON sidecar"
      pattern: "phase-09-report"
---

<objective>
Build the benchmark harness for Phase 9 so temporal PPR can be evaluated against the current
vector baseline on replayed real traces, with token, latency, and temporal-consistency reporting.

Purpose: The roadmap’s value claim for Phase 9 is evidence-backed retrieval quality and token
reduction, not just code that compiles.

Output: `bench/` scripts, smoke fixture, and report-generation path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-temporal-ppr-retrieval-benchmark/09-CONTEXT.md
@.planning/phases/09-temporal-ppr-retrieval-benchmark/09-RESEARCH.md
@.planning/phases/09-temporal-ppr-retrieval-benchmark/09-01-SUMMARY.md
@packages/am-temporal-kg/src/procedures/retrieve.ts
@packages/am-temporal-kg/generated-bindings/index.ts
@src/codememory/chat/pipeline.py
@src/codememory/web/pipeline.py
@src/codememory/web/chunker.py
@src/codememory/server/tools.py
@src/codememory/server/app.py
</context>

<interfaces>
Recommended per-query result row:
```json
{
  "query_id": "q-001",
  "domain": "conversation",
  "baseline_latency_ms": 83,
  "temporal_latency_ms": 121,
  "baseline_token_estimate": 640,
  "temporal_token_estimate": 220,
  "token_reduction_pct": 65.63,
  "baseline_result_count": 8,
  "temporal_result_count": 4,
  "fallback_used": false,
  "temporal_consistent": true
}
```

Recommended output paths:
```text
bench/results/phase-09-report.md
bench/results/phase-09-report.json
```

Current repo token heuristic to preserve for first-pass benchmarking:
```python
int(len(text.split()) * 1.3)
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build replay and query-runner scripts</name>
  <files>
    package.json,
    bench/build_temporal_kg.ts,
    bench/run_queries.ts,
    bench/fixtures/smoke-traces.jsonl
  </files>
  <action>
    Add `bench/build_temporal_kg.ts`.

    This script should:
    - accept an input trace file path,
    - replay conversation/research trace rows into SpacetimeDB using the bridge/helper or
      generated bindings directly,
    - support at least research claims and conversation mention relations,
    - print summary counts for nodes, edges, and evidence rows written.

    Add `bench/run_queries.ts`.

    This script should:
    - accept a query corpus file path,
    - run the same query set against baseline vector retrieval and temporal PPR retrieval,
    - record stage timings for:
      - baseline total,
      - temporal seed discovery,
      - temporal bridge/procedure,
      - temporal hydration/formatting,
    - emit raw JSON result rows for `report_results.ts`.

    Add a tiny smoke fixture at `bench/fixtures/smoke-traces.jsonl`.

    Add root scripts in `package.json`, for example:
    ```json
    "bench:build-temporal": "tsx bench/build_temporal_kg.ts",
    "bench:run-queries": "tsx bench/run_queries.ts",
    "bench:report": "tsx bench/report_results.ts"
    ```
  </action>
  <verify>
    <automated>
      cd /d/code/agentic-memory && npm run typecheck && npx tsx bench/build_temporal_kg.ts --help && npx tsx bench/run_queries.ts --help
    </automated>
  </verify>
  <done>
    The harness can be invoked locally, accepts trace/query inputs, and has a smoke dataset for
    non-private verification.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add token measurement, report generation, and benchmark docs</name>
  <files>
    bench/README.md,
    bench/measure_tokens.ts,
    bench/report_results.ts
  </files>
  <action>
    Add `bench/measure_tokens.ts`.

    Keep the first pass consistent with repo conventions:
    - estimate tokens with the existing `words * 1.3` heuristic,
    - compute on the final context payload text, not raw edge counts.

    Add `bench/report_results.ts`.

    It should:
    - read raw benchmark rows,
    - compute aggregates:
      - mean/p95 latency,
      - average token reduction %,
      - fallback rate,
      - temporal consistency rate,
    - write:
      - `bench/results/phase-09-report.md`
      - `bench/results/phase-09-report.json`

    Add `bench/README.md` documenting:
    - required env vars,
    - how to supply real trace files,
    - what the smoke fixture is for,
    - where reports are written,
    - that real benchmark conclusions should come from replayed real traces, not the smoke fixture.
  </action>
  <verify>
    <automated>
      cd /d/code/agentic-memory && npx tsx bench/report_results.ts --help && npx tsx bench/measure_tokens.ts --help
    </automated>
  </verify>
  <done>
    Benchmark documentation exists. Report generation writes Markdown and JSON. Token estimation is
    consistent with the rest of the repo.
  </done>
</task>

</tasks>

<verification>
```bash
cd /d/code/agentic-memory && npm run typecheck
npx tsx bench/build_temporal_kg.ts --help
npx tsx bench/run_queries.ts --help
npx tsx bench/measure_tokens.ts --help
npx tsx bench/report_results.ts --help
```
Workspace typecheck passes. All benchmark entrypoints are invokable. Report generation path exists.
</verification>

<success_criteria>
- `bench/build_temporal_kg.ts`, `bench/run_queries.ts`, `bench/measure_tokens.ts`, and
  `bench/report_results.ts` exist
- root `package.json` exposes runnable benchmark scripts
- harness supports user-supplied real traces and a committed smoke fixture
- report output is written as Markdown and JSON
- report includes token reduction, latency, fallback usage, and temporal consistency
- workspace typecheck still passes
</success_criteria>

<output>
After completion, create `.planning/phases/09-temporal-ppr-retrieval-benchmark/09-03-SUMMARY.md`
</output>
