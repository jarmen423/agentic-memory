---
phase: 10-cross-module-integration-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/codememory/server/unified_search.py
  - src/codememory/server/result_types.py
  - src/codememory/server/app.py
  - src/codememory/server/tools.py
  - src/am_server/routes/search.py
  - src/am_server/app.py
  - tests/test_unified_search.py
  - tests/test_am_server.py
autonomous: true
requirements:
  - UNIFY-01
  - UNIFY-02
  - UNIFY-03

must_haves:
  truths:
    - "Unified search uses a normalized internal result contract rather than composing formatted tool strings"
    - "`search_all_memory` queries code, web, and conversation modules through structured helpers"
    - "Conversation and research keep their temporal-first + fallback behavior inside unified search"
    - "Code search remains eligible in unified search without fake temporal weighting"
    - "A failure in one module degrades that module only; the overall search still returns healthy results"
  artifacts:
    - path: "src/codememory/server/result_types.py"
      provides: "normalized result types for cross-module retrieval"
      exports:
        - "UnifiedMemoryHit"
        - "UnifiedSearchResponse"
    - path: "src/codememory/server/unified_search.py"
      provides: "cross-module aggregation, normalization, reranking, and trimming"
      exports:
        - "search_all_memory_sync"
    - path: "src/codememory/server/app.py"
      provides: "MCP `search_all_memory` tool"
      contains: "search_all_memory"
    - path: "src/am_server/routes/search.py"
      provides: "REST parity endpoint for unified search"
      contains: "GET /search/all"
  key_links:
    - from: "search_all_memory_sync"
      to: "search_codebase baseline helpers"
      via: "structured code search adapter"
      pattern: "module='code'"
    - from: "search_all_memory_sync"
      to: "conversation temporal retrieval helpers"
      via: "structured conversation search adapter"
      pattern: "module='conversation'"
    - from: "search_all_memory_sync"
      to: "research temporal retrieval helpers"
      via: "structured web search adapter"
      pattern: "module='web'"
---

<objective>
Add the unified retrieval layer for Phase 10 by introducing a normalized cross-module search
contract and exposing `search_all_memory` through the existing server surfaces.

Purpose: the repo already has one MCP server, but no single search surface that merges code,
conversation, and research memory in a deterministic way.

Output: a reusable aggregation service, a new MCP tool, optional REST parity, and tests proving
module-aware fallback behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cross-module-integration-hardening/10-CONTEXT.md
@.planning/phases/10-cross-module-integration-hardening/10-RESEARCH.md
@src/codememory/server/app.py
@src/codememory/server/tools.py
@src/am_server/app.py
@src/am_server/routes/conversation.py
@src/am_server/routes/research.py
</context>

<interfaces>
Recommended request contract:
```text
query: str
project_id: str | None
as_of: str | None
modules: list["code" | "web" | "conversation"] | None
limit: int
```

Recommended normalized result contract:
```text
module
source_kind
source_id
title
excerpt
score
baseline_score
temporal_score
temporal_applied
metadata
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add normalized result models and the unified aggregation service</name>
  <files>
    src/codememory/server/result_types.py,
    src/codememory/server/unified_search.py,
    tests/test_unified_search.py
  </files>
  <behavior>
    - Test: code, web, and conversation hits are normalized into one typed result contract.
    - Test: unified search sorts by normalized score while preserving `temporal_applied` metadata.
    - Test: if one module search raises, the service returns healthy module hits plus error metadata.
    - Test: module filters limit which backends are queried.
  </behavior>
  <action>
    Create a dedicated result model module and a unified search service.

    The service should:
    1. call module-specific structured adapters,
    2. normalize raw module rows into `UnifiedMemoryHit`,
    3. apply a simple, explainable reranking rule,
    4. preserve module-specific metadata and fallback notes,
    5. return a structured response object suitable for both MCP and REST formatting.

    Do not parse existing human-readable tool output. Add structured adapters instead.
  </action>
  <verify>
    <automated>
      cd /d/code/agentic-memory && python -m pytest tests/test_unified_search.py -x -q
    </automated>
  </verify>
  <done>
    Unified search logic is testable outside MCP/REST and supports partial module degradation.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose `search_all_memory` through MCP and REST</name>
  <files>
    src/codememory/server/app.py,
    src/codememory/server/tools.py,
    src/am_server/routes/search.py,
    src/am_server/app.py,
    tests/test_am_server.py,
    tests/test_unified_search.py
  </files>
  <behavior>
    - Test: MCP `search_all_memory` calls the unified service and returns stable tool output.
    - Test: REST `GET /search/all` returns structured JSON results with per-hit module metadata.
    - Test: `as_of` is forwarded to temporal-capable modules only and does not break code results.
    - Test: unified search still returns results when one module falls back or fails.
  </behavior>
  <action>
    Add a thin MCP tool in `src/codememory/server/app.py` and a REST route in
    `src/am_server/routes/search.py`.

    Keep the service boundary clean:
    - MCP tool formats user-facing output from the structured unified response.
    - REST route returns raw structured results.

    Wire the new REST router into `src/am_server/app.py`.
  </action>
  <verify>
    <automated>
      cd /d/code/agentic-memory && python -m pytest tests/test_unified_search.py tests/test_am_server.py -x -q
    </automated>
  </verify>
  <done>
    Unified search is available through both agent and HTTP surfaces without duplicating ranking logic.
  </done>
</task>

</tasks>

<verification>
```bash
cd /d/code/agentic-memory && python -m pytest tests/test_unified_search.py tests/test_am_server.py -x -q
```
Targeted tests prove the normalized result contract, partial failure behavior, and MCP/REST exposure.
</verification>

<success_criteria>
- `search_all_memory` exists as a first-class search surface
- code, web, and conversation results can be merged without parsing formatted tool output
- module-aware fallback behavior is preserved inside unified search
- code results participate without fake temporal scores
- both MCP and REST surfaces are thin wrappers over one shared service
- targeted unified-search tests pass
</success_criteria>
