---
phase: 10-cross-module-integration-hardening
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/codememory/core/runtime_embedding.py
  - src/codememory/core/embedding.py
  - src/codememory/config.py
  - src/am_server/dependencies.py
  - src/codememory/server/tools.py
  - src/codememory/cli.py
  - src/am_server/middleware.py
  - src/codememory/core/retry.py
  - tests/test_embedding_runtime.py
  - tests/test_am_server.py
  - tests/test_cli.py
autonomous: true
requirements:
  - PROVIDER-01
  - HARDEN-01
  - HARDEN-02

must_haves:
  truths:
    - "Embedding provider/model selection for live code paths comes from config/env resolution, not hardcoded Gemini constructors"
    - "Nemotron can be selected end to end for modules that use EmbeddingService"
    - "FastAPI requests carry a correlation id into logs"
    - "Retry behavior is standardized only for transient provider/network boundaries"
    - "Fallback, provider, and bridge events emit consistent structured log fields"
  artifacts:
    - path: "src/codememory/core/runtime_embedding.py"
      provides: "shared embedding runtime resolver/factory"
      exports:
        - "resolve_embedding_runtime"
        - "build_embedding_service"
    - path: "src/am_server/middleware.py"
      provides: "request correlation id middleware"
      contains: "X-Request-ID"
    - path: "src/codememory/core/retry.py"
      provides: "shared retry helpers and transient error classification"
      exports:
        - "retry_transient"
        - "is_transient_provider_error"
  key_links:
    - from: "am_server.dependencies"
      to: "build_embedding_service"
      via: "config-driven module embedding resolution"
      pattern: "module_name='web'|'chat'"
    - from: "codememory.cli"
      to: "build_embedding_service"
      via: "CLI runtime factory reuse"
      pattern: "module_name='web'|'chat'"
    - from: "FastAPI request middleware"
      to: "search/ingest logs"
      via: "request correlation id"
      pattern: "request_id"
---

<objective>
Finish the provider/runtime hardening work for Phase 10 by making embedding selection truly
config-driven and by standardizing request logging and retry behavior across the active services.

Purpose: Nemotron support exists only at the abstraction layer today, while the live factories
still hardcode Gemini. Observability and retry behavior are also inconsistent across the runtime.

Output: shared embedding resolver, live Nemotron-selectable factories, request correlation, and a
small standardized retry/error policy.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-cross-module-integration-hardening/10-CONTEXT.md
@.planning/phases/10-cross-module-integration-hardening/10-RESEARCH.md
@src/codememory/config.py
@src/codememory/core/embedding.py
@src/codememory/core/extraction_llm.py
@src/am_server/dependencies.py
@src/codememory/server/tools.py
@src/codememory/cli.py
</context>

<interfaces>
Recommended resolver output:
```text
provider
api_key
model
dimensions
base_url
module_name
```

Recommended log fields:
```text
event
request_id
module
provider
fallback
error_type
duration_ms
project_id
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace hardcoded embedder construction with a shared runtime resolver</name>
  <files>
    src/codememory/core/runtime_embedding.py,
    src/codememory/core/embedding.py,
    src/codememory/config.py,
    src/am_server/dependencies.py,
    src/codememory/server/tools.py,
    src/codememory/cli.py,
    tests/test_embedding_runtime.py,
    tests/test_cli.py,
    tests/test_am_server.py
  </files>
  <behavior>
    - Test: web/chat/runtime factories read provider/model/dimensions from config rather than hardcoded Gemini values.
    - Test: Nemotron can be selected through config/env and produces an `EmbeddingService` with the expected provider/base_url.
    - Test: extraction-provider wiring remains independent from embedding-provider wiring.
    - Test: existing defaults still resolve to current behavior when config is not overridden.
  </behavior>
  <action>
    Add a shared embedding runtime resolver and refactor all active constructors to use it.

    Update:
    - `src/am_server/dependencies.py`
    - `src/codememory/server/tools.py`
    - `src/codememory/cli.py`

    so web/chat paths no longer instantiate `EmbeddingService(provider="gemini", ...)` directly.
  </action>
  <verify>
    <automated>
      cd /d/code/agentic-memory && python -m pytest tests/test_embedding_runtime.py tests/test_cli.py tests/test_am_server.py -x -q
    </automated>
  </verify>
  <done>
    Live runtime paths use one provider-resolution path, and Nemotron is no longer a dead abstraction.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Standardize request logging and transient retry behavior</name>
  <files>
    src/am_server/middleware.py,
    src/am_server/app.py,
    src/codememory/core/retry.py,
    src/codememory/server/app.py,
    src/codememory/server/tools.py,
    src/codememory/chat/pipeline.py,
    src/codememory/web/pipeline.py,
    tests/test_am_server.py
  </files>
  <behavior>
    - Test: FastAPI responses include a request id header or otherwise expose a stable correlation id.
    - Test: fallback events log consistent structured fields across conversation and web search.
    - Test: transient provider/network failures are retried where appropriate, while validation errors are not retried.
    - Test: existing fallback behavior is preserved.
  </behavior>
  <action>
    Add thin request middleware and shared retry helpers.

    Normalize log events around:
    - temporal bridge unavailable,
    - temporal fallback,
    - provider failure,
    - partial unified-search success,
    - ingest failure.

    Limit retry usage to clearly transient transport/provider boundaries.
  </action>
  <verify>
    <automated>
      cd /d/code/agentic-memory && python -m pytest tests/test_am_server.py -x -q
    </automated>
  </verify>
  <done>
    Runtime behavior is diagnosable across modules, and transient external failures have one shared policy.
  </done>
</task>

</tasks>

<verification>
```bash
cd /d/code/agentic-memory && python -m pytest tests/test_embedding_runtime.py tests/test_cli.py tests/test_am_server.py -x -q
```
Tests prove live provider resolution, preserve defaults, and cover request-context logging behavior.
</verification>

<success_criteria>
- no active web/chat embedding factory hardcodes Gemini directly
- Nemotron can be selected through config/env in live runtime paths
- request-scoped correlation ids exist for FastAPI flows
- fallback and provider failures log consistently across modules
- retry policy is shared and limited to transient boundaries
- targeted provider/runtime hardening tests pass
</success_criteria>
