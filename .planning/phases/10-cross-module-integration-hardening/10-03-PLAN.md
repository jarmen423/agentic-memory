---
phase: 10-cross-module-integration-hardening
plan: "03"
type: execute
wave: 2
depends_on:
  - 10-01-PLAN.md
  - 10-02-PLAN.md
files_modified:
  - tests/integration/test_full_stack_search.py
  - tests/integration/conftest.py
  - pytest.ini
  - README.md
  - docs/SETUP_FULL_STACK.md
  - docs/MCP_TOOL_REFERENCE.md
  - docs/PROVIDER_CONFIGURATION.md
  - docs/SPACETIMEDB_OPERATIONS.md
  - .planning/phases/10-cross-module-integration-hardening/10-UAT.md
autonomous: true
requirements:
  - TEST-01
  - TEST-02
  - DOCS-01

must_haves:
  truths:
    - "A live integration suite covers code, web, and conversation retrieval from one running app surface"
    - "Fallback behavior is validated end to end, not only by unit tests"
    - "A new contributor can start Neo4j, SpacetimeDB, and am-server from documentation without reconstructing the workflow from chat history"
    - "MCP tool and provider docs reflect the post-Phase-10 runtime behavior"
  artifacts:
    - path: "tests/integration/test_full_stack_search.py"
      provides: "cross-module live integration coverage"
      contains: "search_all_memory"
    - path: "docs/SETUP_FULL_STACK.md"
      provides: "operator runbook for local full-stack startup"
    - path: "docs/MCP_TOOL_REFERENCE.md"
      provides: "documented MCP surfaces including search_all_memory"
    - path: "docs/PROVIDER_CONFIGURATION.md"
      provides: "embedding and extraction provider configuration guide"
    - path: "docs/SPACETIMEDB_OPERATIONS.md"
      provides: "SpacetimeDB publish/start/bridge/sync worker troubleshooting"
  key_links:
    - from: "tests/integration/test_full_stack_search.py"
      to: "docs/SETUP_FULL_STACK.md"
      via: "same startup assumptions and env contract"
      pattern: "Neo4j + SpacetimeDB + am-server"
    - from: "docs/PROVIDER_CONFIGURATION.md"
      to: "runtime embedding/extraction resolvers"
      via: "config examples"
      pattern: "Nemotron/Gemini/OpenAI"
---

<objective>
Close Phase 10 with end-to-end validation and operator documentation so the repo is testable and
usable as one integrated system rather than a collection of individually verified modules.

Purpose: the roadmap explicitly requires cross-module integration tests and setup documentation.
Those are the proof that the prior plans actually produced a coherent product.

Output: an integration suite, setup/provider/operator docs, and a Phase 10 UAT artifact.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cross-module-integration-hardening/10-CONTEXT.md
@.planning/phases/10-cross-module-integration-hardening/10-RESEARCH.md
@src/am_server/app.py
@src/codememory/server/app.py
@src/codememory/server/tools.py
@packages/am-temporal-kg/README.md
@packages/am-sync-neo4j/README.md
</context>

<interfaces>
Required operator workflow to document:
```text
1. start Neo4j
2. start SpacetimeDB
3. publish/generate temporal module if needed
4. start am-sync-neo4j if required by the scenario
5. start am-server
6. exercise MCP/REST search surfaces
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add full-stack integration tests for unified search and fallback paths</name>
  <files>
    tests/integration/conftest.py,
    tests/integration/test_full_stack_search.py,
    pytest.ini
  </files>
  <behavior>
    - Test: booted app returns unified results spanning at least code + one memory module.
    - Test: conversation and/or research temporal path participates when the bridge is available.
    - Test: deterministic fallback still returns stable result shapes when the temporal bridge is disabled or unavailable.
    - Test: provider/config overrides used in integration scenarios are documented and reproducible.
  </behavior>
  <action>
    Add a live integration suite with clear markers and environment requirements.

    Prefer tests that verify:
    - app boot,
    - minimal ingest/setup,
    - `search_all_memory`,
    - fallback behavior,
    - response-shape stability.

    Keep external-provider dependence minimal where deterministic local fixtures can stand in.
  </action>
  <verify>
    <automated>
      cd /d/code/agentic-memory && python -m pytest tests/integration/test_full_stack_search.py -m integration -q
    </automated>
  </verify>
  <done>
    The repo has one clear integration suite proving the full search story works beyond unit tests.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Write full-stack setup, MCP, provider, and SpacetimeDB operations docs</name>
  <files>
    README.md,
    docs/SETUP_FULL_STACK.md,
    docs/MCP_TOOL_REFERENCE.md,
    docs/PROVIDER_CONFIGURATION.md,
    docs/SPACETIMEDB_OPERATIONS.md,
    .planning/phases/10-cross-module-integration-hardening/10-UAT.md
  </files>
  <behavior>
    - Docs describe the exact local startup order and env requirements.
    - Docs explain how embedding-provider selection differs from extraction-provider selection.
    - Docs include the new `search_all_memory` surface and fallback expectations.
    - Phase 10 UAT tracks live validation criteria aligned with the roadmap.
  </behavior>
  <action>
    Add focused docs instead of one giant README dump.

    Minimum doc set:
    - `docs/SETUP_FULL_STACK.md`
    - `docs/MCP_TOOL_REFERENCE.md`
    - `docs/PROVIDER_CONFIGURATION.md`
    - `docs/SPACETIMEDB_OPERATIONS.md`

    Update `README.md` to point to the new docs and full-stack flow.
    Create `10-UAT.md` with explicit verification steps for final phase closure.
  </action>
  <verify>
    <manual>
      Follow docs from a fresh terminal sequence and confirm the startup order plus unified search verification are reproducible without relying on chat history.
    </manual>
  </verify>
  <done>
    The repo becomes operable by a new contributor using committed docs rather than ad hoc prior-session knowledge.
  </done>
</task>

</tasks>

<verification>
```bash
cd /d/code/agentic-memory && python -m pytest tests/integration/test_full_stack_search.py -m integration -q
```
Then follow `docs/SETUP_FULL_STACK.md` verbatim from a fresh terminal sequence and confirm the
documented startup flow is sufficient.
</verification>

<success_criteria>
- live integration coverage exists for unified search and fallback behavior
- docs cover full-stack startup, MCP tools, provider selection, and SpacetimeDB operations
- a new contributor can reproduce local startup without reconstructing prior chat steps
- Phase 10 has its own UAT artifact for final closure
</success_criteria>
