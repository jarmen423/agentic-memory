{
  "name": "agentic-memory-workspace",
  "private": true,
  "workspaces": [
    "packages/am-temporal-kg",
    "packages/am-sync-neo4j",
    "packages/am-dashboard",
    "packages/am-openclaw"
  ],
  "scripts": {
    "build": "npm run build --workspace am-temporal-kg && npm run build --workspace am-sync-neo4j && npm run build --workspace packages/am-openclaw",
    "typecheck": "npm run typecheck --workspace am-temporal-kg && npm run typecheck --workspace am-sync-neo4j && npm run typecheck --workspace packages/am-openclaw",
    "lint": "npm run lint --workspace am-temporal-kg && npm run lint --workspace am-sync-neo4j && npm run lint --workspace packages/am-openclaw",
    "test:openclaw": "npm run test --workspace packages/am-openclaw",
    "build:openclaw": "npm run build --workspace packages/am-openclaw",
    "typecheck:openclaw": "npm run typecheck --workspace packages/am-openclaw",
    "pack:openclaw": "npm pack --workspace packages/am-openclaw --dry-run",
    "validate:release-artifacts": "docker compose -f docker-compose.prod.yml config",
    "bench:build-temporal": "tsx bench/build_temporal_kg.ts",
    "bench:run-queries": "tsx bench/run_queries.ts",
    "bench:tokens": "tsx bench/measure_tokens.ts",
    "bench:report": "tsx bench/report_results.ts"
  }
}
